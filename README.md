# wheel-of-name

- Màn hình quay: `/`
- Control Room: `/admin/`
- Mật khẩu giao diện admin: `be9`

Control Room dùng `localStorage` để đồng bộ giữa các tab cùng origin. Đây là khóa
giao diện phía client, không phải lớp xác thực bảo mật cho môi trường công khai.
